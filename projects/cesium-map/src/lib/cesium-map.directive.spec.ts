import { CesiumMapDirective } from './cesium-map.directive';

describe('CesiumMapDirective', () => {
  it('should create an instance', () => {
    const directive = new CesiumMapDirective();
    expect(directive).toBeTruthy();
  });
});
